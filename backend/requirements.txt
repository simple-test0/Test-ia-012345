# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
websockets==13.1

# Database
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.20.0

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# ML - Core
# GPU (NVIDIA): pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
# AMD ROCm:    --extra-index-url https://download.pytorch.org/whl/rocm6.2
# CPU / Apple: plain `pip install torch torchvision` (MPS ships in the default macOS wheel)
# The unsuffixed pins below resolve to the CPU build by default; the index URL
# above selects the matching CUDA/ROCm build. (torchaudio tracks torch's 2.x line.)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Image Generation (FLUX.1 / SD3.5 require diffusers>=0.31)
diffusers==0.32.2
transformers==4.46.3
accelerate==1.1.1
safetensors==0.5.2
sentencepiece==0.2.0    # T5 text encoder (SD3.5 / FLUX)
protobuf==5.28.3
# Optional, NVIDIA-only — SDPA is used by default so this is not required:
# xformers==0.0.28.post3

# Agents / LLM
httpx==0.28.1
ddgs==9.14.4

# Labs / Training
datasets==3.1.0
huggingface-hub==0.26.2
kaggle==1.6.17
onnx==1.17.0
onnxruntime==1.20.1

# Hardware detection
gputil==1.4.0
psutil==6.0.0

# Utilities
Pillow==10.4.0
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
structlog==24.4.0
anyio==4.6.0
aiofiles==24.1.0
