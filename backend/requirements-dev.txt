# Test runners
pytest==8.3.3
pytest-asyncio==0.24.0
httpx>=0.27          # async test client for FastAPI

# Type checking
mypy>=1.10
types-psutil         # stubs for psutil used in hardware detector

# Pre-commit (local dev only — not used in CI)
pre-commit>=3.7
