target-version = "py311"
line-length = 100

# Default rules (pyflakes F + pycodestyle E/W) plus import sorting (I).
[lint]
select = ["E4", "E7", "E9", "F", "I"]

[lint.per-file-ignores]
# Migrations are autogenerated.
"migrations/**" = ["E", "F", "I"]
